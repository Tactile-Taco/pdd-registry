{
  "artifact_id": "taxonomy-web-service-python-stdlib",
  "protocol": {"name": "web-service", "version": "1.0.0", "status": "sealed"},
  "language": "python",
  "runtime": "python-3.12",
  "files": [
    "web_service_vocab.py",
    "tests/test_web_service_vocab.py"
  ],
  "dependencies": {"runtime": ["stdlib only"], "dev": ["pytest"]},
  "entry_module": "web_service_vocab",
  "entry_class": null,
  "smoke": {
    "method": "validate_against",
    "args": {"components": {"api": "surface"}},
    "assert_expr": "r == []"
  },
  "benchmark": {
    "method": "validate_against",
    "iterations": 1000,
    "args_template": {"components": {"api": "x", "database": "y"}}
  },
  "mutation_sanity": {
    "invariant": "B-001",
    "pytest_filter": "B001",
    "find": "        if name not in VOCABULARY:",
    "replace": "        if False:  # MUTANT: unknown-component guard removed (B-001)"
  },
  "invariant_lineage": {
    "B-001": ["test_B001_known_components_pass", "test_B001_unknown_component_reported", "test_B001_unknown_template_ref_reported", "test_B001_non_dict_components_fail", "test_B001_deterministic_and_non_mutating"]
  },
  "known_limitations": [
    "Validates the vocabulary SHAPE only; the taxonomy does not enforce conformance of concrete bundles",
    "Composition with taxonomy/ai-agent is document-level (depends_on edge); no cross-taxonomy checks in this candidate"
  ],
  "self_check": "non-authoritative: run locally, still a candidate until the Validation Engine admits"
}
